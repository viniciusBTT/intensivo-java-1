services:
  mysql:
    image: mysql:8.4
    container_name: intensivo-mysql
    environment:
      MYSQL_ROOT_PASSWORD: senha234
      MYSQL_DATABASE: backend
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
